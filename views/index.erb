<div class="row">
  <div class="col s12 m12 l12 xl12 center-align">
    <h2><%= @title %></h2>
    <a class="btn" href='./new'>新規作成</a>
  </div>
</div>

<div class="row">
  <div class="col s12 m12 l12 xl12 center-align">
    <ul>
      <% @memos.each do |memo| %>
        <li>
            <a href='./<%= h(memo[0]) %>'>
              <%= h(memo[0]) %>
            </a>
        </li>
      <% end %>
    </ul>
  </div>
</div>
